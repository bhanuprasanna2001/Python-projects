# GitHub API Client Configuration
# Copy this file to .env and fill in your values
# The client will automatically read GITHUB_TOKEN from environment

# Your GitHub Personal Access Token
# Create one at: https://github.com/settings/tokens
# Required scopes depend on what you want to do:
#   - No scope: public read-only access
#   - repo: full repository access (private repos)
#   - user: read/write user profile
GITHUB_TOKEN=ghp_your_personal_access_token_here

# Optional: GitHub Enterprise URL (default: https://api.github.com)
# GITHUB_BASE_URL=https://github.yourcompany.com/api/v3

# Optional: Request timeout in seconds (default: 30)
# GITHUB_TIMEOUT=30

# Optional: Maximum retry attempts (default: 3)
# GITHUB_MAX_RETRIES=3

# Optional: Cache TTL in seconds (default: 300)
# GITHUB_CACHE_TTL=300
